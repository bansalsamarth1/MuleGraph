apiVersion: v1
kind: Service
metadata:
  name: {{ include "mulegraph.fullname" . }}
  labels:
    {{- include "mulegraph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.type "NodePort") .Values.nodePort }}
      nodePort: {{ .Values.nodePort }}
      {{- end }}
  selector:
    {{- include "mulegraph.selectorLabels" . | nindent 4 }}
